// Configuracao de ambiente para desenvolvimento local.
// O proxy em proxy.conf.json redireciona /api/* para http://localhost:8080.
export const environment = {
  production: false,
  apiUrl: '/api',
};
